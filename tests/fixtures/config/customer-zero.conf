[core]
    engine = pg
    top_dir = ./db
    deploy_dir = db/deploy
    revert_dir = db/revert
    verify_dir = db/verify
[engine "pg"]
[target "localtest"]
    uri = db:pg://postgres:Secret@localhost:5460/postgres_ai
