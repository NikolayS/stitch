# This is a comment
; This is also a comment

[core]
    # Comment inside section
    engine = pg  # inline comment
    top_dir = ./src  ; another inline comment

[deploy]
    verify = yes
    ; mode is the default
    mode = change
